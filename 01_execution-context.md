# Thread of Execution
The JavaScript interpreter executes code line-by-line. Variables or "data" like strings and arrays are saved in memory. Functions can also be saved and executed at a later time.

```
const num = 3; // declare a const num and value 3 into memory
function multiplyBy2 (inputNumber) {
 const result = inputNumber * 2;
 return result;
}
```
